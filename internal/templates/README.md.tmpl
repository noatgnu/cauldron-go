# Nextflow Pipeline: {{ .ToolName }}

This pipeline was automatically generated from a CauldronGO plugin.

## Description
{{ .Description }}

## Requirements
- Nextflow (>= 22.10.0)
- Docker OR Singularity OR Conda

## Usage

### Run with Docker (Local)
```bash
nextflow run main.nf -profile docker --input 'path/to/data.tsv'
```

### Build from Dockerfile (Optional)
If you want to build the container locally instead of pulling it:
```bash
docker build -t {{ .ContainerImageDocker }} -f containers/Dockerfile .
```

### Run without cloning (Remote)
If this plugin is hosted on GitHub, you can run it directly:
```bash
nextflow run <github_user>/<repo_name> -r nextflow -profile docker --input 'path/to/samplesheet.csv'
```

## Integration as a Module
You can include this plugin as a step in your own custom Nextflow pipeline:

```nextflow
include { {{ .ProcessName | upper }} } from './path/to/{{ .ProcessName }}/modules/local/{{ .ProcessName }}/main'

workflow {
    {{ .ProcessName | upper }}(ch_input, ...)
}
```

### Parameters
{{ range .Inputs }}
- `--{{ .Name }}`: {{ .Description }} (Default: {{ .Default }})
{{- end }}

## Plugin Metadata
- **ID**: {{ .ProcessName }}
- **Version**: {{ .Version }}
- **Author**: {{ .Author }}

Generated by [CauldronGO](https://github.com/noatgnu/cauldron-go)
